import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.xixo.random',
  appName: 'Xixo Random Picker',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
